# planning

Create actionable plans per project based on current state and strategy.

→ reads `projects/`, `logs/`, `strategy.md` → writes `todo/{date}.md`

<!-- impl-wanted: branch `impl/manage-duck-planning` to add your implementation -->
