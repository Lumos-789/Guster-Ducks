# Terminal（终端氛围）

Make the terminal look and feel great — prompt, colors, transparency, font.

## Acceptance

- Sets up a theme that works in both light and dark mode
- Configures prompt with git branch, exit code, execution time
- Enables terminal transparency or blur if the emulator supports it
- Persists across sessions and new windows

## Implementation

Empty on this branch.
