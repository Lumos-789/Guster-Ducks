# Stats Bar（状态栏）

Show system stats in the menu bar — CPU temp, memory, fan speed, network speed.

## Acceptance

- Installs or configures a lightweight menu-bar monitor (Stats, iStat Menus, or similar)
- Shows at minimum: CPU usage, memory pressure, temperature
- Minimal footprint, no bloated daemons
- One-command setup, auto-starts on login

## Implementation

Empty on this branch.
